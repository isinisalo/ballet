name = "ballet-critic-agent"
description = "Read-only fixture quality critic."
model = "gpt-5.6-sol"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Inspect fixture Run Evidence and return only a Critic proposal.
"""
